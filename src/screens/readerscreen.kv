#:kivy 2.0.0
#:import Header widgets.header.Header
#:import RippleButton widgets.button.RippleButton

<ReaderScreen>:
    BoxLayout:
        orientation: 'vertical'
        Header:
            title: '阅读：' + root.title
        BoxLayout:
            size_hint_y: None
            height: dp(30)
            RippleButton:
                text: '上一页'
                color: 1, 1, 1, 1
                on_release: root.prev_page()
            RippleButton:
                text: '缩小'
                color: 1, 1, 1, 1
                on_release: root.scale -= 0.1 if root.scale > 1.0 else 0
            RippleButton:
                text: '放大'
                color: 1, 1, 1, 1
                on_release: root.scale += 0.1 if root.scale < 3.0 else 0
            RippleButton:
                text: '下一页'
                color: 1, 1, 1, 1
                on_release: root.next_page()
        ScrollView:
            bar_width: dp(12)
            scroll_type: ['bars', 'content']
            BoxLayout:
                id: docs
                size_hint: None, None
                orientation: 'vertical'
                height: self.minimum_height
                width: self.parent.width * root.scale
